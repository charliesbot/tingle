# binaries
/tingle
/cmd/tingle/tingle
*.exe

# Go build cache / testing artifacts
/dist/
*.test
*.out
coverage.*

# editor / os
.DS_Store
.idea/
.vscode/
*.swp
*~

# Generated map — every `tingle` run regenerates it, so committing would
# pin stale diffs to every commit. Matches the `.gitignore` nudge the CLI
# prints on first encounter.
.tinglemap.md

# scratch / spike output
/scratch/
/spikes/*/out/

# Spike build artifacts (Go binaries)
/spikes/a_perf/spike
/spikes/b_regex/tingle-regex
/spikes/b_utility/tingle
/spikes/d_puregoTS/spike
evals/results/
